CREATE FUNCTION ACCOUNT_EXIST_BY_ID (
	@ID int
) RETURNS INT AS
BEGIN 
	IF (SELECT COUNT(A.ID_Account) FROM ACCOUNTS A WHERE A.ID_Account = @ID) =0
	BEGIN 
		RETURN 0
	END
	RETURN (SELECT A.ID_Account FROM ACCOUNTS A WHERE A.ID_Account = @ID)
END
